# iOS Shortcut

The future share endpoint accepts a URL, an optional owner-authored one-line reason, and optional shared text. Authentication and exact import instructions will be published when the capture service exists.

Phase 1 does not expose an endpoint and does not include a token.
